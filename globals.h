#ifndef GLOBALS_H
#define GLOBALS_H

#include <QString>
#include <QGlobalStatic>
#include "APIcfBase.h"

// глобальный файл конфигурации, по идее он должен быть один...
//Q_GLOBAL_STATIC(std::unique_ptr<v8catalog>, GlobalCF) // Объявляем глобальную строку
//Q_GLOBAL_STATIC(v8catalog, GlobalCF); // Объявляем глобальную строку

#endif // GLOBALS_H
